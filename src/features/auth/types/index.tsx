// src/types/auth.types.ts

export interface PersonalInfoFormValues {
    fullName: string;
    phoneNumber: string;
    city: string;
    vehicleType: string;
  }
  
  export interface DocumentSubmissionFormValues {
    // Add document submission fields here when needed
  }
  
  export interface VehicleRequirementsFormValues {
    // Add vehicle requirements fields here when needed
  }